dependencies:
  idf:
    version: ">=5.0"
  # esp_gcov for coverage support (experimental in QEMU)
  # Note: esp_gcov requires OpenOCD for dumping, which is not available in QEMU.
  # Coverage data collection will be attempted but may not work without JTAG.
  espressif/esp_gcov:
    version: "^1"
